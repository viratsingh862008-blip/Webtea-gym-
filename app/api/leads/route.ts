import{NextResponse}from"next/server";import{validateLead}from"@/lib/validation";
export async function POST(request:Request){try{const checked=validateLead(await request.json());if(!checked.ok)return NextResponse.json({ok:false,errors:checked.errors},{status:400});const lead={id:crypto.randomUUID(),...checked.value,createdAt:new Date().toISOString()};const webhook=process.env.LEAD_WEBHOOK_URL;if(webhook)await fetch(webhook,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(lead),cache:"no-store"});return NextResponse.json({ok:true,leadId:lead.id,message:"Your request is in. Kingdom Gym can follow up with you.",forwarded:Boolean(webhook)})}catch{return NextResponse.json({ok:false,message:"Could not process your request."},{status:500})}}