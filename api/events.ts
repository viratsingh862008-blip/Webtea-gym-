export default async function handler(req:any,res:any){if(req.method!=="POST")return res.status(405).json({ok:false});try{const event=req.body;const webhook=process.env.ANALYTICS_WEBHOOK_URL;if(webhook)await fetch(webhook,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...event,receivedAt:new Date().toISOString()})});return res.status(200).json({ok:true})}catch{return res.status(400).json({ok:false})}}