export type Lead={name:string;phone:string;goal:string;preferredTime:string};
export function validateLead(input:Partial<Lead>){const name=String(input.name??"").trim();const phone=String(input.phone??"").trim();const goal=String(input.goal??"").trim();const errors:Record<string,string>={};if(name.length<2)errors.name="Enter your name.";if(!/^[+0-9()\-\s]{8,18}$/.test(phone))errors.phone="Enter a valid phone number.";if(!goal)errors.goal="Choose a goal.";return{ok:Object.keys(errors).length===0,errors,value:{name,phone,goal,preferredTime:String(input.preferredTime??"").trim()}}}